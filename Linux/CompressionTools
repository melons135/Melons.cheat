% unzip
# Unzip password protected zip file
unzip -P <password> -d <target directory> <file>

# List contents of zip file
unzip -l <file>

# Update existing file and create fif necessary
unzip -u -d <target directory> <file>

# Do not overwrite any files
unzip -n -d <target directory> <file>

# Freshen existing files but create none
unzip -f -d <target directory> <file>

# Exclude file from extraction
unzip <file> -x <exclude file>
